#include <stdio.h>

int main() {
    int n, a = 0, b = 1, next;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("Fibonacci series up to %d:\n", n);

    while (b <= n) {
        printf("%d ", b);
        next = a + b;
        a = b;
        b = next;
    }

    return 0;
}
